'use strict';

describe('gitHubExplorer module', function() {

  beforeEach(module('gitHubExplorer'));

  describe('gitHubExplorer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});